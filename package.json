{
  "dependencies": {
    "@react-pdf/renderer": "^4.3.0",
    "@types/js-yaml": "^4.0.9",
    "js-yaml": "^4.1.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "name": "cc-resume-manager",
  "type": "module",
  "private": false,
  "license": "CC-BY-NC-4.0",
  "scripts": {
    "dev": "bun run generate-data.ts && bun --hot index.html",
    "start": "bun run generate-data.ts && bun index.html",
    "save-to-pdf": "echo 'Usage: bun run generate-data.ts -C company-name && bun run generate-pdf.ts'; exit 1",
    "generate-data": "bun run generate-data.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
