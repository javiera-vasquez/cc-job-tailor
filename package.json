{
  "version": "0.5.0",
  "dependencies": {
    "@react-pdf/renderer": "^4.3.0",
    "@types/js-yaml": "^4.0.9",
    "js-yaml": "^4.1.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "name": "cc-resume-manager",
  "type": "module",
  "private": false,
  "license": "CC-BY-NC-4.0",
  "scripts": {
    "dev": "bun dev-with-watch.ts",
    "dev:basic": "bun --hot index.html",
    "start": "bun run generate-data.ts && bun index.html",
    "save-to-pdf": "bun run generate-data.ts \"$@\" && bun run generate-pdf.ts \"$@\"",
    "generate-data": "bun run generate-data.ts",
    "typecheck": "bun run tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
