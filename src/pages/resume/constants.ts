// Design system constants for resume components
// Centralized design tokens to avoid circular imports

// Design system colors based on Figma specifications
export const colors = {
  primary: '#000000',      // Black - primary text
  darkGray: '#292929',     // Dark gray - content text  
  mediumGray: '#545454',   // Medium gray - secondary text
  separatorGray: '#A6A6A6', // Separator lines
};

// Typography scales (can be extended in the future)
export const typography = {
  // Could add font sizes, line heights, letter spacing here
};

// Spacing scales (can be extended in the future)
export const spacing = {
  // Could add consistent spacing values here
};